<?xml version="1.0" encoding="utf-8"?>
<unattend xmlns="urn:schemas-microsoft-com:unattend"
          xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

  <!-- ============================================================ -->
  <!-- 1. WINDOWS PE: idioma + disco + selección de imagen          -->
  <!-- ============================================================ -->
  <settings pass="windowsPE">

    <component name="Microsoft-Windows-International-Core-WinPE"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">
      <SetupUILanguage>
        <UILanguage>es-ES</UILanguage>
      </SetupUILanguage>
      <UILanguage>es-ES</UILanguage>
      <UserLocale>es-ES</UserLocale>
      <SystemLocale>es-ES</SystemLocale>
      <InputLocale>es-ES</InputLocale>
    </component>

    <component name="Microsoft-Windows-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">

      <!-- Particionado UEFI en disco 0 -->
      <DiskConfiguration>
        <Disk wcm:action="add">
          <DiskID>0</DiskID>
          <WillWipeDisk>true</WillWipeDisk>

          <CreatePartitions>
            <!-- EFI -->
            <CreatePartition wcm:action="add">
              <Order>1</Order>
              <Type>EFI</Type>
              <Size>100</Size>
            </CreatePartition>

            <!-- MSR -->
            <CreatePartition wcm:action="add">
              <Order>2</Order>
              <Type>MSR</Type>
              <Size>16</Size>
            </CreatePartition>

            <!-- Primary (C:) -->
            <CreatePartition wcm:action="add">
              <Order>3</Order>
              <Type>Primary</Type>
              <Extend>true</Extend>
            </CreatePartition>
          </CreatePartitions>

          <ModifyPartitions>
            <!-- EFI FAT32 -->
            <ModifyPartition wcm:action="add">
              <PartitionID>1</PartitionID>
              <Order>1</Order>
              <Label>System</Label>
              <Format>FAT32</Format>
            </ModifyPartition>

            <!-- C: NTFS -->
            <ModifyPartition wcm:action="add">
              <PartitionID>3</PartitionID>
              <Order>2</Order>
              <Label>Windows</Label>
              <Format>NTFS</Format>
              <Letter>C</Letter>
            </ModifyPartition>
          </ModifyPartitions>

        </Disk>
      </DiskConfiguration>

      <ImageInstall>
        <OSImage>
          <!-- Instalar en disco 0, partición 3 -->
          <InstallTo>
            <DiskID>0</DiskID>
            <PartitionID>3</PartitionID>
          </InstallTo>

          <!-- Índice del install.wim -->
          <InstallFrom>
            <MetaData wcm:action="add">
              <Key>/IMAGE/INDEX</Key>
              <Value>{{ vm_image_index }}</Value>
            </MetaData>
          </InstallFrom>

          <WillShowUI>OnError</WillShowUI>
        </OSImage>
      </ImageInstall>

      <UserData>
        <AcceptEula>true</AcceptEula>
        <FullName>Administrador</FullName>
        <Organization>Empresa</Organization>
      </UserData>

    </component>
  </settings>

  <!-- ============================================================ -->
  <!-- 2. SPECIALIZE: solo hostname                                -->
  <!-- ============================================================ -->
  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">

      <ComputerName>{{ vm_name }}</ComputerName>

    </component>
  </settings>

  <!-- ============================================================ -->
  <!-- 3. OOBE SYSTEM: contraseña + autologon + IP estática         -->
  <!-- ============================================================ -->
  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-Shell-Setup"
               processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35"
               language="neutral"
               versionScope="nonSxS">

      <!-- Password de Administrator -->
      <UserAccounts>
        <AdministratorPassword>
          <Value>{{ admin_password }}</Value>
          <PlainText>true</PlainText>
        </AdministratorPassword>
      </UserAccounts>

      <!-- Autologon una sola vez -->
      <AutoLogon>
        <Username>Administrator</Username>
        <Enabled>true</Enabled>
        <LogonCount>1</LogonCount>
        <Password>
          <Value>{{ admin_password }}</Value>
          <PlainText>true</PlainText>
        </Password>
      </AutoLogon>

      <!-- Primer inicio: esperar NIC y configurar IP estática -->
      <FirstLogonCommands>
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Description>Configurar IP estática en Ethernet</Description>
          <CommandLine>
            powershell.exe -ExecutionPolicy Bypass -NoProfile -Command "
              Start-Sleep -Seconds 8;
              $ErrorActionPreference = 'SilentlyContinue';
              $i = 0;
              while ($i -lt 5) {
                $nic = Get-NetAdapter -Name 'Ethernet' -ErrorAction SilentlyContinue;
                if ($nic -and $nic.Status -eq 'Up') {
                  New-NetIPAddress -InterfaceAlias 'Ethernet' -IPAddress '{{ vm_ip }}' -PrefixLength {{ vm_prefix }} -DefaultGateway '{{ vm_gateway }}';
                  Set-DnsClientServerAddress -InterfaceAlias 'Ethernet' -ServerAddresses '{{ vm_dns1 }}','{{ vm_dns2 }}';
                  break;
                }
                Start-Sleep -Seconds 3;
                $i++;
              }
            "
          </CommandLine>
        </SynchronousCommand>
      </FirstLogonCommands>

    </component>
  </settings>

</unattend>
